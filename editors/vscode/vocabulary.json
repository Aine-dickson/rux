{
  "version": "0.7.0",
  "elements": [
    { "name": "screen", "detail": "the root element of a document", "doc": "The root of a `.rux` document. A component whose template root is `<screen>` is a page; anything else is a fragment." },
    { "name": "view", "detail": "a box; the generic container", "doc": "A generic box, the element most markup is made of. Defaults to `display: block`, so give it `display: flex` to lay its children out." },
    { "name": "text", "detail": "a run of text", "doc": "A run of text. Two `<text>` siblings **stack**: there is no inline flow, so they do not share a line. `role=\"heading\"` announces it as a heading." },
    { "name": "image", "detail": "an image from a file", "doc": "`src` resolves relative to the `.rux` file, not the working directory. PNG, JPEG, GIF and WebP. With no CSS size it lays out at its intrinsic pixel size." },
    { "name": "path", "detail": "vector geometry from SVG path data", "doc": "`d` is SVG path data, in the element's own coordinates. Paint is CSS, not attributes: `fill`, `stroke`, `stroke-width`, `stroke-linecap`, `stroke-linejoin`, `fill-rule`. With no CSS size it lays out at the size of its own geometry. `:d` binds an expression, and two paths with the same command sequence animate between one another under `transition: d`." },
    { "name": "button", "detail": "a tappable box, announced as a button", "doc": "A tappable box. `<view @tap>` is the same thing to the layout; `<button>` says so to the accessibility tree." },
    { "name": "input", "detail": "text field, textarea, select, checkbox or radio", "doc": "A text field by default. `type=\"textarea\"` is multiline, `type=\"select\"` a combo box, `type=\"checkbox\"` and `type=\"radio\"` carry live checked state. Bind it with `r-model`." },
    { "name": "slot", "detail": "a component's hole for the caller's children", "doc": "Renders whatever the caller wrote between the component's tags. Children written inside `<slot>…</slot>` are the default, used when the caller passed nothing." },
    { "name": "router", "detail": "renders the one matching route", "doc": "Renders the first `<route>` whose path matches. Leaves no box of its own: the matched view expands in its place." },
    { "name": "route", "detail": "maps a path to a component", "doc": "`<route path=\"/crew/:id\" view=\"crew-detail\" />`. Routes are tried in the order written and the first match wins, so `fallback` can sit anywhere among them." }
  ],
  "globalAttributes": [
    { "name": "@tap", "detail": "run script when this element is tapped", "doc": "A click and a finger are the same event. `@tap=\"count += 1\"` takes an expression, and the element becomes focusable and announces as tappable." },
    { "name": "@press", "detail": "a finger or button went down", "doc": "Runs when a press lands on this element. `event.x` / `event.y` are relative to the element, `event.pageX` / `event.pageY` to the window; `event.touches` lists every finger down." },
    { "name": "@release", "detail": "the press came up", "doc": "Runs on release, whether or not the press stayed still enough to be a tap." },
    { "name": "@longpress", "detail": "the press rested", "doc": "Runs once, after the press has been held still for half a second." },
    { "name": "@swipe", "detail": "a flick, with a direction", "doc": "Runs once at the end of a press that travelled far enough, fast enough. `event.direction` is `left`, `right`, `up` or `down`, and `event.totalX` / `event.totalY` are how far it came. A drag that ends as a flick fires this too." },
    { "name": "@drag", "detail": "the pointer is moving", "doc": "Runs at the start of a drag, on every move, and at the end. `event.phase` says which; `event.totalX` / `event.totalY` measure from where the press landed, `event.moveX` / `event.moveY` from the previous event. A `@drag` claims the finger, so the page under it does not scroll." },
    { "name": "class", "detail": "CSS classes", "doc": "Space-separated class names, matched by `.name` selectors. `:class` is the bound form." },
    { "name": "id", "detail": "unique id", "doc": "Matched by `#name` selectors, and by `query(\"#name\")` from script." },
    { "name": "style", "detail": "inline declarations", "doc": "Inline CSS for this element. `:style` is the bound form." },
    { "name": "role", "detail": "accessibility role", "doc": "Honored for selectors (`[role=\"heading\"]`) and for the accessibility tree. Matches case-insensitively." },
    { "name": "to", "detail": "make this element a link", "doc": "Tapping navigates to this path, the element announces as a link, and it matches `:current` when it names the path you are on. `:to` is the bound form." }
  ],
  "directives": [
    { "name": "r-for", "detail": "repeat this element per item", "doc": "`r-for=\"item in items\"`. Pair it with `r-key` so reconciliation can match rows across a change." },
    { "name": "r-key", "detail": "identity for a repeated row", "doc": "`r-key=\"item.id\"`. Without it a keyed change is a rebuild, and input state inside a row moves to the wrong row." },
    { "name": "r-if", "detail": "render only when true", "doc": "`r-if=\"count > 0\"`. The element and its subtree are absent, not hidden, when false." },
    { "name": "r-elif", "detail": "an else-if branch", "doc": "Follows an `r-if` sibling. The first branch whose condition holds renders." },
    { "name": "r-else", "detail": "the fallback branch", "doc": "Follows an `r-if` or `r-elif` sibling, and renders when none of them held." },
    { "name": "r-show", "detail": "hide without removing", "doc": "`r-show=\"expanded\"`. The element stays in the tree and keeps its state; it is only not painted. Prefer `r-if` when the subtree is expensive." },
    { "name": "r-model", "detail": "two-way bind an input", "doc": "`r-model=\"name\"` on an `<input>`: typing writes the signal, and writing the signal updates the field." },
    { "name": "r-transition", "detail": "animate the way in and out", "doc": "On an `r-if` branch or a keyed `r-for` row: the element is held on screen while it leaves. Style the two sides with `:enter-from` and `:leave-to`; the element's own `transition` sets how long. `:r-transition=\"expr\"` hands progress to you instead, 0 to 1." }
  ],
  "elementAttributes": {
    "image": [
      { "name": "src", "detail": "path, relative to this .rux file", "doc": "Resolved relative to the document, not the working directory. `:src` binds an expression." },
      { "name": "alt", "detail": "accessible description", "doc": "What the image is, for the accessibility tree." }
    ],
    "path": [
      { "name": "d", "detail": "SVG path data", "doc": "The full SVG grammar: `M L H V C S Q T A Z`, absolute or relative. Coordinates are in the element's own box. `:d` binds an expression." },
      { "name": "alt", "detail": "accessible description", "doc": "What the drawing shows. Without it the path is treated as decoration and left out of the accessibility tree." }
    ],
    "input": [
      { "name": "type", "detail": "text | textarea | select | checkbox | radio", "doc": "Omitted means a single-line text field." },
      { "name": "placeholder", "detail": "text shown while empty", "doc": "Shown until the field has a value. Not a label." },
      { "name": "value", "detail": "the field's value", "doc": "The literal starting value. For state that changes, use `r-model`." },
      { "name": "checked", "detail": "checkbox / radio state", "doc": "Live state for `type=\"checkbox\"` and `type=\"radio\"`, and matched by the `:checked` pseudo-class." },
      { "name": "name", "detail": "radio group", "doc": "Radios sharing a `name` are one group, so choosing one clears the others." },
      { "name": "options", "detail": "the choices for a select", "doc": "For `type=\"select\"`. `:options` binds an array." }
    ],
    "route": [
      { "name": "path", "detail": "the path to match", "doc": "`/crew/:id` captures `id` and hands it to the view as a prop. A child route's path is relative to its parent, and `path=\"\"` is the index route that fills the parent's outlet at the parent's own path." },
      { "name": "view", "detail": "the component to render", "doc": "Names an imported component, the same name its tag would use." },
      { "name": "fallback", "detail": "match anything unmatched", "doc": "Valueless. Renders when no other route matched, wherever it sits among them." },
      { "name": "guard", "detail": "decide whether this route may be entered", "doc": "`guard=\"expr\"`, run whenever this route is part of the match. `false` cancels the navigation, a string redirects to that path, and anything else allows it, `()` included, so a function that falls off the end has consented. Outer guards run first." }
    ],
    "router": [
      { "name": "restore-scroll", "detail": "restore scroll position on Back", "doc": "Puts a page back where it was when you return to it." },
      { "name": "guard", "detail": "decide whether any navigation may proceed", "doc": "`guard=\"expr\"`, run on every navigation this router handles, before the history moves. So a refusal leaves no entry behind, and Back and Forward pass through it too, not just `navigate`. `false` cancels, a string redirects, anything else allows." }
    ],
    "text": [
      { "name": "for", "detail": "label another element", "doc": "Names the `id` of the element this text labels, so the accessibility tree pairs them." }
    ]
  },
  "scriptGlobals": [
    { "name": "signal", "detail": "signal(value)", "doc": "Declare a piece of reactive state." },
    { "name": "computed", "detail": "computed name = expr;", "doc": "Derived state, readable anywhere a signal is. A **declaration, not a call**: there is no `computed(|| …)`. Refreshing is one pass in declaration order, so a computed may read one declared above it and not below. Inside a component it runs per instance." },
    { "name": "effect", "detail": "effect { … }", "doc": "Runs when what it read changes, and once on load. A **block, not a call**: there is no `effect(|| …)`. It subscribes to what it actually read on its last run, and is never woken by its own writes." },
    { "name": "mounted", "detail": "mounted { … }", "doc": "Runs once the document is on screen. Document level today; a component declaring one is warned." },
    { "name": "unmounted", "detail": "unmounted { … }", "doc": "Runs when the document stops being the one on screen." },
    { "name": "query", "detail": "query(\"selector\")", "doc": "Find elements by CSS selector, in a handler, against the frame already laid out. Returns an **array**, so check `.length` or index it: a selector that matches nothing is an empty array, not an error. Only inside a handler, because a binding that read the tree would rebuild the tree it read." },
    { "name": "navigate", "detail": "navigate(path)", "doc": "Go to a path, leaving the current one in the history." },
    { "name": "replace", "detail": "replace(path)", "doc": "Go to a path *without* a history entry. The only correct way to redirect." },
    { "name": "back", "detail": "back()", "doc": "Walk back through the history." },
    { "name": "forward", "detail": "forward()", "doc": "Walk forward through the history." },
    { "name": "path_for", "detail": "path_for(\"route\", #{ … })", "doc": "Build a path from a named route and its parameters." },
    { "name": "emit", "detail": "emit(\"name\", payload)", "doc": "A component telling its caller something happened." },
    { "name": "blur", "detail": "blur()", "doc": "Drop focus. Free-standing rather than a method on an element, because there is only one focused element: blurring \"this one\" would either do nothing or take focus from something else." },
    { "name": "setInterval", "detail": "setInterval(ms) { … }", "doc": "Run a block on a period, handing back a handle. A **block, not a callback**: `setInterval(1000) { seconds++ }`. Stop it with `clearInterval(handle)`." },
    { "name": "clearInterval", "detail": "clearInterval(handle)", "doc": "Stop a timer started by `setInterval`. The handle is what `setInterval` returned." },
    { "name": "print", "detail": "print(x)", "doc": "Printf-debugging. Reaches the dev overlay, not just stderr." },
    { "name": "debug", "detail": "debug(x)", "doc": "Like `print`, with the value's structure shown." }
  ],
  "elementProperties": [
    { "name": "tag", "detail": "the element's tag name", "doc": "`\"view\"`, `\"text\"`, and so on." },
    { "name": "id", "detail": "its `id`, or absent", "doc": "**Absent rather than empty** when it has none, so `el.id ?? \"none\"` reads the way it does everywhere else." },
    { "name": "classes", "detail": "its classes, as an array", "doc": "Every class on the element, in the order written." },
    { "name": "x", "detail": "left edge, in pixels", "doc": "From the frame **currently on screen**, so it is one frame stale. That is the guarantee, not a defect: a handler runs before the next layout, exactly as `getBoundingClientRect` does. Absent when there is no frame yet, so \"not laid out\" stays distinguishable from \"laid out and genuinely zero\"." },
    { "name": "y", "detail": "top edge, in pixels", "doc": "See `x`: one frame stale, absent before the first layout." },
    { "name": "width", "detail": "width, in pixels", "doc": "See `x`: one frame stale, absent before the first layout." },
    { "name": "height", "detail": "height, in pixels", "doc": "See `x`: one frame stale, absent before the first layout." }
  ],
  "elementMethods": [
    { "name": "focus", "detail": "focus()", "doc": "Give this element keyboard focus. Recorded and applied **after the handler finishes**, so a handler that focuses something and then changes state does not race its own tree." },
    { "name": "scrollIntoView", "detail": "scrollIntoView()", "doc": "Scroll whichever box scrolls until this element is visible. Applied after the handler, like the rest." },
    { "name": "tap", "detail": "tap()", "doc": "Tap this element as though a finger had. This is how a `mounted` hook drives the document, and how most of the lifecycle was verified." }
  ],
  "valueMethods": [
    { "name": "map", "detail": "map(|x| …)", "doc": "A new array, each item transformed. Returns; the receiver is untouched." },
    { "name": "filter", "detail": "filter(|x| …)", "doc": "A new array of the items that passed." },
    { "name": "reduce", "detail": "reduce(|acc, x| …, start)", "doc": "Fold the array to one value." },
    { "name": "forEach", "detail": "forEach(|item, index| …)", "doc": "Called with `(item, index)`, and falls back to `(item)`." },
    { "name": "find", "detail": "find(|x| …)", "doc": "The first item that passed, or absent." },
    { "name": "includes", "detail": "includes(x)", "doc": "Whether the array or string contains it." },
    { "name": "indexOf", "detail": "indexOf(x)", "doc": "Where it first occurs, or -1." },
    { "name": "slice", "detail": "slice(start, end)", "doc": "A section, without changing the original." },
    { "name": "split", "detail": "split(sep)", "doc": "A string to an array of pieces." },
    { "name": "join", "detail": "join(sep)", "doc": "An array to one string." },
    { "name": "charAt", "detail": "charAt(i)", "doc": "The character at an index." },
    { "name": "repeat", "detail": "repeat(n)", "doc": "The string over again, n times." },
    { "name": "startsWith", "detail": "startsWith(s)", "doc": "Whether it begins with that." },
    { "name": "endsWith", "detail": "endsWith(s)", "doc": "Whether it ends with that." },
    { "name": "trim", "detail": "trim()", "doc": "**Returns** the trimmed string and leaves the receiver alone. rhai's own `trim` empties the string it was given and returns nothing, which rendered `{{ name.trim() }}` blank; Rux shadows it." },
    { "name": "toLowerCase", "detail": "toLowerCase()", "doc": "Returns a new string." },
    { "name": "toUpperCase", "detail": "toUpperCase()", "doc": "Returns a new string." },
    { "name": "length", "detail": "length", "doc": "A **property, not `len()`**, and only on arrays and strings." }
  ],
  "pseudoClasses": [
    { "name": "hover", "detail": "the pointer is over this box", "doc": "Needs a layout region, so the shell can tell when the pointer enters and leaves it. There is no hover on a touch screen, so it can decorate and must not be the only way to reach something." },
    { "name": "focus", "detail": "this element has the keyboard", "doc": "Reached by tapping a field or by walking the focus order. Worth styling: a focus ring is the only thing on screen that says where typing will go." },
    { "name": "active", "detail": "a press is down on this box", "doc": "Held between press and release, so it is the state a button uses to look pushed." },
    { "name": "checked", "detail": "a checkbox or radio is on", "doc": "Resolved during the build from the input's live state, rather than supplied by the shell as `:hover` and `:active` are." },
    { "name": "current", "detail": "this link names the path you are on", "doc": "Matches an element whose `to` is the route you are currently on. It is the nav highlight, without a signal to track it." },
    { "name": "enter-from", "detail": "the frame an element enters from", "doc": "Under `r-transition`: the style the element holds for exactly one frame as it arrives, and animates away from. The *hidden* end of the animation goes here." },
    { "name": "leave-to", "detail": "the state an element leaves to", "doc": "Under `r-transition`: the style the element animates towards while it is held on screen on its way out. `position: absolute` here takes it out of flow, so the rest of a list closes up under it rather than waiting." }
  ],
  "voidTags": [
    "image", "input", "path", "router-view", "area", "base",
    "br", "col", "embed", "hr", "img", "link",
    "meta", "param", "source", "track", "wbr"
  ],
  "cssProperties": [
    "display", "width", "height", "gap", "min-width", "max-width",
    "min-height", "max-height", "padding", "padding-top", "padding-right", "padding-bottom",
    "padding-left", "margin", "margin-top", "margin-right", "margin-bottom", "margin-left",
    "border", "border-width", "border-color", "border-radius", "border-top-left-radius", "border-top-right-radius",
    "border-bottom-right-radius", "border-bottom-left-radius", "border-top", "border-right", "border-bottom", "border-left",
    "border-top-width", "border-right-width", "border-bottom-width", "border-left-width", "overflow", "overflow-x",
    "overflow-y", "opacity", "cursor", "box-shadow", "transform", "transition",
    "flex", "flex-grow", "flex-shrink", "flex-basis", "flex-wrap", "flex-direction",
    "justify-content", "align-items", "align-self", "justify-self", "justify-items", "align-content",
    "row-gap", "column-gap", "grid-template-columns", "grid-template-rows", "grid-column", "grid-row",
    "grid-column-start", "grid-column-end", "grid-row-start", "grid-row-end", "grid-auto-flow", "grid-auto-rows",
    "grid-auto-columns", "position", "top", "right", "bottom", "left",
    "aspect-ratio", "background", "background-color", "background-image", "color", "font-size",
    "font-weight", "font-family", "font-style", "text-align", "letter-spacing", "word-spacing",
    "line-height", "white-space", "text-decoration", "text-decoration-line", "overflow-wrap", "word-wrap",
    "word-break", "fill", "fill-rule", "stroke", "stroke-width", "stroke-linecap",
    "stroke-linejoin"
  ],
  "cssPropertyDocs": [
    { "name": "display", "detail": "how this box lays its children out", "doc": "`block` (the default), `flex`, `grid`, `inline` or `none`. **Rux defaults to `block`**, so a box does not lay its children out in a row until you say `display: flex`.", "usage": ".row { display: flex; }" },
    { "name": "width", "detail": "how wide the box is", "doc": "A length or a percentage of the parent's width. `100%` is the usual way to make a child fill a flex parent, because Rux's cross-axis default is `flex-start` rather than CSS's `stretch`.", "usage": ".field { width: 100%; }" },
    { "name": "height", "detail": "how tall the box is", "doc": "A length or a percentage. A definite height is half of what makes a scroller; the other half is `overflow`.", "usage": ".thread { height: 300px; }" },
    { "name": "gap", "detail": "space between children", "doc": "On a flex or grid parent. Space *between* items only, never outside them, which is what makes it different from margin.", "usage": ".row { display: flex; gap: 8px; }" },
    { "name": "min-width", "detail": "a width it will not go under", "doc": "Stops a flex item shrinking past this. `min-width: 0` is the escape hatch when an item refuses to shrink at all.", "usage": ".cell { min-width: 0; }" },
    { "name": "max-width", "detail": "a width it will not go over", "doc": "Caps the box however much room there is. Common on text so a line does not run the full window.", "usage": ".msg { max-width: 320px; }" },
    { "name": "min-height", "detail": "a height it will not go under", "doc": "Useful on a scroller that must keep its size when empty.", "usage": ".panel { min-height: 120px; }" },
    { "name": "max-height", "detail": "a height it will not go over", "doc": "Caps growth; pair with `overflow` to scroll the excess rather than clip it.", "usage": ".menu { max-height: 240px; overflow-y: auto; }" },
    { "name": "padding", "detail": "space inside the border, all four sides", "doc": "One to four lengths, clockwise from the top. Inside the background, so padding is painted in the box's own colour.", "usage": ".card { padding: 12px 16px; }" },
    { "name": "padding-top", "detail": "space inside the top edge", "doc": "Overrides the `padding` shorthand for this side.", "usage": ".card { padding-top: 12px; }" },
    { "name": "padding-right", "detail": "space inside the right edge", "doc": "Overrides the `padding` shorthand for this side.", "usage": ".card { padding-right: 16px; }" },
    { "name": "padding-bottom", "detail": "space inside the bottom edge", "doc": "Overrides the `padding` shorthand for this side.", "usage": ".card { padding-bottom: 12px; }" },
    { "name": "padding-left", "detail": "space inside the left edge", "doc": "Overrides the `padding` shorthand for this side.", "usage": ".card { padding-left: 16px; }" },
    { "name": "margin", "detail": "space outside the box, all four sides", "doc": "One to four lengths, clockwise from the top. Outside the background, so it shows the parent through.", "usage": ".card { margin: 0 auto; }" },
    { "name": "margin-top", "detail": "space outside the top edge", "doc": "Overrides the `margin` shorthand for this side.", "usage": ".card { margin-top: 8px; }" },
    { "name": "margin-right", "detail": "space outside the right edge", "doc": "Overrides the `margin` shorthand for this side.", "usage": ".card { margin-right: 8px; }" },
    { "name": "margin-bottom", "detail": "space outside the bottom edge", "doc": "Overrides the `margin` shorthand for this side.", "usage": ".card { margin-bottom: 8px; }" },
    { "name": "margin-left", "detail": "space outside the left edge", "doc": "Overrides the `margin` shorthand for this side.", "usage": ".card { margin-left: 8px; }" },
    { "name": "border", "detail": "width, style and colour of the outline", "doc": "`2px solid #45475a`. The style word is accepted and only the width and colour are drawn.", "usage": ".field { border: 2px solid #45475a; }" },
    { "name": "border-width", "detail": "how thick the outline is", "doc": "Takes room in the layout, so changing it moves the content inside.", "usage": ".field { border-width: 2px; }" },
    { "name": "border-color", "detail": "what colour the outline is", "doc": "Animatable, so it is the usual way to show focus without the box moving.", "usage": ".field:focus { border-color: #89b4fa; }" },
    { "name": "border-radius", "detail": "how round the corners are", "doc": "One to four lengths. Clips the background and the border; it does **not** clip children unless the box also has `overflow: hidden`.", "usage": ".card { border-radius: 12px; }" },
    { "name": "border-top-left-radius", "detail": "roundness of one corner", "doc": "Overrides `border-radius` for this corner.", "usage": ".bubble { border-top-left-radius: 2px; }" },
    { "name": "border-top-right-radius", "detail": "roundness of one corner", "doc": "Overrides `border-radius` for this corner.", "usage": ".bubble { border-top-right-radius: 2px; }" },
    { "name": "border-bottom-right-radius", "detail": "roundness of one corner", "doc": "Overrides `border-radius` for this corner.", "usage": ".bubble { border-bottom-right-radius: 2px; }" },
    { "name": "border-bottom-left-radius", "detail": "roundness of one corner", "doc": "Overrides `border-radius` for this corner.", "usage": ".bubble { border-bottom-left-radius: 2px; }" },
    { "name": "border-top", "detail": "the top edge's width and colour", "doc": "The one-side form of the `border` shorthand.", "usage": ".footer { border-top: 1px solid #313244; }" },
    { "name": "border-right", "detail": "the right edge's width and colour", "doc": "The one-side form of the `border` shorthand.", "usage": ".sidebar { border-right: 1px solid #313244; }" },
    { "name": "border-bottom", "detail": "the bottom edge's width and colour", "doc": "The one-side form of the `border` shorthand. A cheap way to draw a divider under a row.", "usage": ".row { border-bottom: 1px solid #313244; }" },
    { "name": "border-left", "detail": "the left edge's width and colour", "doc": "The one-side form of the `border` shorthand. A cheap way to draw a quote bar.", "usage": ".quote { border-left: 3px solid #89b4fa; }" },
    { "name": "border-top-width", "detail": "thickness of the top edge", "doc": "Set on its own, leaving the colour to the shorthand.", "usage": ".row { border-top-width: 1px; }" },
    { "name": "border-right-width", "detail": "thickness of the right edge", "doc": "Set on its own, leaving the colour to the shorthand.", "usage": ".row { border-right-width: 1px; }" },
    { "name": "border-bottom-width", "detail": "thickness of the bottom edge", "doc": "Set on its own, leaving the colour to the shorthand.", "usage": ".row { border-bottom-width: 1px; }" },
    { "name": "border-left-width", "detail": "thickness of the left edge", "doc": "Set on its own, leaving the colour to the shorthand.", "usage": ".row { border-left-width: 3px; }" },
    { "name": "overflow", "detail": "what happens to content too big for the box", "doc": "`auto` or `scroll` makes this box **the scroller**; `hidden` or `clip` cuts the overflow off. **Nothing in Rux scrolls until a box is told to**, and the box told is the one with a definite size.", "usage": ".thread { height: 300px; overflow: auto; }" },
    { "name": "overflow-x", "detail": "the same, for the horizontal axis only", "doc": "Use when a row should scroll sideways while the page does not.", "usage": ".tabs { overflow-x: auto; }" },
    { "name": "overflow-y", "detail": "the same, for the vertical axis only", "doc": "The usual half: a list that scrolls while the screen around it stays put.", "usage": ".thread { height: 300px; overflow-y: auto; }" },
    { "name": "opacity", "detail": "how see-through the whole box is", "doc": "`0` to `1`, and it applies to the **subtree**, not just this box. Animatable, and the cheapest thing to animate.", "usage": ".msg:enter-from { opacity: 0; }" },
    { "name": "cursor", "detail": "the pointer shape over this box", "doc": "Only `pointer` differs from the default arrow. Applied by the shell on hover.", "usage": ".send { cursor: pointer; }" },
    { "name": "box-shadow", "detail": "a shadow cast by the box", "doc": "`x y blur colour`. Painted outside the border and takes no space in the layout.", "usage": ".modal { box-shadow: 0 8px 24px #00000066; }" },
    { "name": "transform", "detail": "move, rotate or scale the painted box", "doc": "`translate`, `rotate`, `scale`, composed left to right. Paint-only, so it never moves anything else. **A transformed box becomes the containing block** for `absolute` *and* `fixed` descendants, which is why a `fixed` child stops being fixed inside one.", "usage": ".msg:enter-from { transform: translateY(10px); }" },
    { "name": "transition", "detail": "animate a property when it changes", "doc": "`<property> <duration> <easing>`, comma-separated for more than one. Only some properties can animate; naming one that cannot is a warning rather than silence.", "usage": ".msg { transition: opacity 180ms ease-out; }" },
    { "name": "flex", "detail": "grow, shrink and basis in one", "doc": "`flex: 1` means `1 1 0%`, not `1 1 auto`. The usual way to say \\\"take the leftover room\\\".", "usage": ".field { flex: 1; }" },
    { "name": "flex-grow", "detail": "share of the spare room this item takes", "doc": "`0` (the default) means it never grows.", "usage": ".field { flex-grow: 1; }" },
    { "name": "flex-shrink", "detail": "how readily this item gives room up", "doc": "`0` stops it shrinking below its content.", "usage": ".icon { flex-shrink: 0; }" },
    { "name": "flex-basis", "detail": "the size to start from before growing", "doc": "`auto` uses the item's own size; a length overrides it.", "usage": ".col { flex-basis: 200px; }" },
    { "name": "flex-wrap", "detail": "whether items may move onto another line", "doc": "`wrap` or `wrap-reverse` allow it; anything else keeps one line.", "usage": ".tags { display: flex; flex-wrap: wrap; }" },
    { "name": "flex-direction", "detail": "which way the children are laid out", "doc": "`row` (the default) or `column`. This is the axis every alignment property below is described against.", "usage": ".app { display: flex; flex-direction: column; }" },
    { "name": "justify-content", "detail": "alignment along the main axis", "doc": "The axis `flex-direction` set. On a `row`, this is horizontal.", "usage": ".row { display: flex; justify-content: space-between; }" },
    { "name": "align-items", "detail": "alignment across the main axis", "doc": "On a `row`, this is vertical. **Rux defaults to `flex-start`, not CSS's `stretch`**, so a child hugs its content unless told to fill.", "usage": ".row { display: flex; align-items: center; }" },
    { "name": "align-self", "detail": "this one item's cross-axis alignment", "doc": "Overrides the parent's `align-items` for this item only. `align-self: flex-end` is how one row in a list moves to the far side.", "usage": ".mine { align-self: flex-end; }" },
    { "name": "justify-self", "detail": "this one item's main-axis alignment", "doc": "Grid mostly; a single item's override.", "usage": ".cell { justify-self: center; }" },
    { "name": "justify-items", "detail": "the default main-axis alignment for children", "doc": "Grid mostly; set on the parent.", "usage": ".grid { justify-items: start; }" },
    { "name": "align-content", "detail": "how whole lines are spaced", "doc": "Only does anything with more than one line, so it needs `flex-wrap: wrap`.", "usage": ".wrapped { flex-wrap: wrap; align-content: flex-start; }" },
    { "name": "row-gap", "detail": "space between rows", "doc": "Overrides `gap` on the vertical axis.", "usage": ".grid { row-gap: 12px; }" },
    { "name": "column-gap", "detail": "space between columns", "doc": "Overrides `gap` on the horizontal axis.", "usage": ".grid { column-gap: 8px; }" },
    { "name": "grid-template-columns", "detail": "the column tracks", "doc": "`repeat(3, 1fr)`, `200px 1fr`, and so on. Setting this is most of what makes a grid.", "usage": ".grid { display: grid; grid-template-columns: repeat(3, 1fr); }" },
    { "name": "grid-template-rows", "detail": "the row tracks", "doc": "Explicit row sizes; rows beyond these come from `grid-auto-rows`.", "usage": ".grid { grid-template-rows: auto 1fr auto; }" },
    { "name": "grid-column", "detail": "which columns this item spans", "doc": "`grid-column: 1 / 3`, or `span 2`.", "usage": ".wide { grid-column: 1 / 3; }" },
    { "name": "grid-row", "detail": "which rows this item spans", "doc": "`grid-row: 1 / 3`, or `span 2`.", "usage": ".tall { grid-row: span 2; }" },
    { "name": "grid-column-start", "detail": "the column this item starts at", "doc": "The longhand half of `grid-column`.", "usage": ".cell { grid-column-start: 2; }" },
    { "name": "grid-column-end", "detail": "the column this item ends at", "doc": "The longhand half of `grid-column`.", "usage": ".cell { grid-column-end: 4; }" },
    { "name": "grid-row-start", "detail": "the row this item starts at", "doc": "The longhand half of `grid-row`.", "usage": ".cell { grid-row-start: 1; }" },
    { "name": "grid-row-end", "detail": "the row this item ends at", "doc": "The longhand half of `grid-row`.", "usage": ".cell { grid-row-end: 3; }" },
    { "name": "grid-auto-flow", "detail": "how items without a place are filled in", "doc": "`row` or `column`, plus `dense` to backfill gaps.", "usage": ".grid { grid-auto-flow: column; }" },
    { "name": "grid-auto-rows", "detail": "the size of rows nobody declared", "doc": "Applies to rows created beyond `grid-template-rows`.", "usage": ".grid { grid-auto-rows: 80px; }" },
    { "name": "grid-auto-columns", "detail": "the size of columns nobody declared", "doc": "Applies to columns created beyond `grid-template-columns`.", "usage": ".grid { grid-auto-columns: 1fr; }" },
    { "name": "position", "detail": "how this box is placed", "doc": "`static` (the default), `relative`, `sticky`, `absolute` or `fixed`. **All five are honored as of v0.7**; before that four of them parsed and silently behaved as `relative`.", "usage": ".heading { position: sticky; top: 0; }" },
    { "name": "top", "detail": "distance from the top edge", "doc": "An offset for `relative`/`absolute`/`fixed`, and a **threshold** for `sticky`: where it stops and holds.", "usage": ".heading { position: sticky; top: 0; }" },
    { "name": "right", "detail": "distance from the right edge", "doc": "An offset, or a sticky threshold. `absolute` measures from the nearest positioned ancestor, not the parent.", "usage": ".badge { position: absolute; right: 8px; }" },
    { "name": "bottom", "detail": "distance from the bottom edge", "doc": "An offset, or a sticky threshold.", "usage": ".bar { position: fixed; bottom: 0; }" },
    { "name": "left", "detail": "distance from the left edge", "doc": "An offset, or a sticky threshold.", "usage": ".badge { position: absolute; left: 8px; }" },
    { "name": "aspect-ratio", "detail": "width-to-height ratio to keep", "doc": "`16 / 9`. Gives a box a height derived from its width, so a media box keeps its shape as it resizes.", "usage": ".cover { width: 100%; aspect-ratio: 16 / 9; }" },
    { "name": "background", "detail": "the colour or gradient behind the content", "doc": "A colour, `linear-gradient(…)` or `radial-gradient(…)`. `url()` is not supported; use `<image>` for a picture.", "usage": ".app { background: #1e1e2e; }" },
    { "name": "background-color", "detail": "a flat colour behind the content", "doc": "Animatable, so it is what a hover or pressed state usually changes.", "usage": ".send:hover { background-color: #b4befe; }" },
    { "name": "background-image", "detail": "a gradient behind the content", "doc": "`linear-gradient` and `radial-gradient` only.", "usage": ".hero { background-image: linear-gradient(#89b4fa, #1e1e2e); }" },
    { "name": "color", "detail": "the colour of the text", "doc": "Inherited, so setting it on a container covers the text inside. Animatable.", "usage": ".label { color: #cdd6f4; }" },
    { "name": "font-size", "detail": "how large the text is", "doc": "Also the reference for `em` on this box, so changing it moves anything sized in `em`.", "usage": ".title { font-size: 22px; }" },
    { "name": "font-weight", "detail": "how heavy the text is", "doc": "`normal`, `bold`, `lighter`, `bolder`, or a number from 100 to 900.", "usage": ".title { font-weight: 700; }" },
    { "name": "font-family", "detail": "which typeface to use", "doc": "Falls back through the list to whatever the system has.", "usage": ".app { font-family: Inter, sans-serif; }" },
    { "name": "font-style", "detail": "upright or italic", "doc": "`italic` and `oblique` both slant; anything else is upright.", "usage": ".empty { font-style: italic; }" },
    { "name": "text-align", "detail": "how lines sit within the box", "doc": "`start`, `center`, `end`/`right`, or `justify`. Aligns the **lines inside** the box, which is not the same as moving the box.", "usage": ".title { text-align: center; }" },
    { "name": "letter-spacing", "detail": "extra space between characters", "doc": "A length, positive or negative.", "usage": ".caps { letter-spacing: 0.5px; }" },
    { "name": "word-spacing", "detail": "extra space between words", "doc": "A length, positive or negative.", "usage": ".lead { word-spacing: 1px; }" },
    { "name": "line-height", "detail": "the height of one line of text", "doc": "A number (a multiple of the font size) or a length. The usual way to give body text room to breathe.", "usage": ".body { line-height: 20px; }" },
    { "name": "white-space", "detail": "whether runs of space collapse and lines wrap", "doc": "`nowrap` and `pre` keep the text on one line; anything else wraps normally.", "usage": ".label { white-space: nowrap; }" },
    { "name": "text-decoration", "detail": "underline or strike-through", "doc": "`underline` and `line-through` are drawn.", "usage": ".link { text-decoration: underline; }" },
    { "name": "text-decoration-line", "detail": "which decoration to draw", "doc": "The longhand of `text-decoration`.", "usage": ".done { text-decoration-line: line-through; }" },
    { "name": "overflow-wrap", "detail": "whether a long word may break mid-word", "doc": "`break-word` or `anywhere` let an unbreakable word break rather than overflow.", "usage": ".body { overflow-wrap: break-word; }" },
    { "name": "word-wrap", "detail": "the older name for `overflow-wrap`", "doc": "Accepted as a synonym.", "usage": ".body { word-wrap: break-word; }" },
    { "name": "word-break", "detail": "how aggressively words break", "doc": "`break-all` breaks anywhere at all, not only to avoid an overflow.", "usage": ".code { word-break: break-all; }" },
    { "name": "fill", "detail": "the colour inside the path", "doc": "On `<path>`. Paint is CSS here rather than an SVG attribute, which is what gives a path `:hover`, `:class` and `transition`. Animatable.", "usage": ".mark { fill: #f9e2af; }" },
    { "name": "fill-rule", "detail": "which parts count as inside", "doc": "`nonzero` (the default) or `evenodd`. The difference shows on a shape that crosses itself, like a star.", "usage": ".star { fill-rule: evenodd; }" },
    { "name": "stroke", "detail": "the colour of the outline", "doc": "On `<path>`. Animatable.", "usage": ".mark { stroke: #1e1e2e; }" },
    { "name": "stroke-width", "detail": "how thick the outline is", "doc": "On `<path>`. Animatable, so a line can thicken on hover.", "usage": ".mark { stroke-width: 2; }" },
    { "name": "stroke-linecap", "detail": "the shape of a line's ends", "doc": "`butt` (the default), `round` or `square`.", "usage": ".mark { stroke-linecap: round; }" },
    { "name": "stroke-linejoin", "detail": "the shape where two segments meet", "doc": "`miter` (the default), `round` or `bevel`.", "usage": ".mark { stroke-linejoin: round; }" }
  ],
  "cssValues": {
    "display": ["block", "flex", "grid", "inline", "none"],
    "position": ["static", "relative", "sticky", "absolute", "fixed"],
    "overflow": ["visible", "hidden", "clip", "auto", "scroll"],
    "overflow-x": ["visible", "hidden", "clip", "auto", "scroll"],
    "overflow-y": ["visible", "hidden", "clip", "auto", "scroll"],
    "flex-direction": ["row", "column"],
    "flex-wrap": ["nowrap", "wrap", "wrap-reverse"],
    "flex": ["none", "auto", "initial"],
    "flex-basis": ["auto", "content"],
    "justify-content": ["flex-start", "start", "center", "flex-end", "end", "space-between", "space-around"],
    "align-content": ["flex-start", "start", "center", "flex-end", "end", "space-between", "space-around"],
    "align-items": ["flex-start", "start", "center", "flex-end", "end", "stretch"],
    "align-self": ["flex-start", "start", "center", "flex-end", "end", "stretch"],
    "justify-self": ["flex-start", "start", "center", "flex-end", "end", "stretch"],
    "justify-items": ["flex-start", "start", "center", "flex-end", "end", "stretch"],
    "grid-auto-flow": ["row", "column", "row dense", "column dense"],
    "text-align": ["left", "start", "center", "right", "end", "justify"],
    "font-weight": ["normal", "bold", "lighter", "bolder", "100", "200", "300", "400", "500", "600", "700", "800", "900"],
    "font-style": ["normal", "italic", "oblique"],
    "white-space": ["normal", "nowrap", "pre"],
    "text-decoration": ["none", "underline", "line-through"],
    "text-decoration-line": ["none", "underline", "line-through"],
    "overflow-wrap": ["normal", "break-word", "anywhere"],
    "word-wrap": ["normal", "break-word", "anywhere"],
    "word-break": ["normal", "break-all"],
    "cursor": ["default", "pointer"],
    "fill-rule": ["nonzero", "evenodd"],
    "stroke-linecap": ["butt", "round", "square"],
    "stroke-linejoin": ["miter", "round", "bevel"]
  },
  "animatableProperties": [
    "all", "opacity", "background-color", "color", "border-color", "border-width",
    "border-radius", "width", "height", "padding", "margin", "gap",
    "font-size", "transform", "inset", "fill", "stroke", "stroke-width",
    "d"
  ],
  "easings": [
    "linear", "ease", "ease-in", "ease-out", "ease-in-out"
  ]
}
