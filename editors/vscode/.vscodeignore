.vscode/**
node_modules/**
*.vsix
.gitignore
# The tests run from the repo (`node --test`), and shipping them would put a
# `require('../vocabulary')` in the package that resolves outside it.
test/**
